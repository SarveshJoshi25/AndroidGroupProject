package com.example.androidmicroproject.model;

public class Temperature {
     public final int min, max, current;

    public Temperature(int min, int max, int current) {
        this.min = min;
        this.max = max;
        this.current = current;
    }
}
